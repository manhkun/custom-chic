<link rel="stylesheet" href="{{ 'section-product-slider.css' | asset_url }}" type="text/css" media="all">
<link rel="stylesheet" href="{{ 'component-slider.css' | asset_url }}" type="text/css" media="all">
<link rel="stylesheet" href="{{ 'component-card.css' | asset_url }}" type="text/css" media="all">
<noscript>{{ 'component-card.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'section-product-slider.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-slider.css' | asset_url | stylesheet_tag }}</noscript>

{%- style -%}
   [data-section-id="{{ section.id }}"].gp-product-slider-wrapper {
    background: {{ section.settings.background_color }};
  }
  [data-section-id="{{ section.id }}"].pt-{{ section.settings.padding_top }}-{{ section.settings.padding_top_mb }}{
    padding-top: {{ section.settings.padding_top_mb }}px;
  }
  [data-section-id="{{ section.id }}"].pb-{{ section.settings.padding_bottom }}-{{ section.settings.padding_bottom_mb }}{
    padding-bottom: {{ section.settings.padding_bottom_mb }}px;
  }
  @media screen and (min-width: 1024px) {
    [data-section-id="{{ section.id }}"].pt-{{ section.settings.padding_top }}-{{ section.settings.padding_top_mb }}{
      padding-top: {{ section.settings.padding_top }}px;
    }
    [data-section-id="{{ section.id }}"].pb-{{ section.settings.padding_bottom }}-{{ section.settings.padding_bottom_mb }}{
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div data-section-id="{{ section.id }}" data-section-type="gp-product-slider" class="gp-section-wrapper gp-product-slider-wrapper pt-{{ section.settings.padding_top }}-{{ section.settings.padding_top_mb }} pb-{{ section.settings.padding_bottom }}-{{ section.settings.padding_bottom_mb }}">
  <div class="gp-product-slider page-width-desktop gp-section-inner collection">
    <div class="row">
      <div class="col-12 position-{{ forloop.index }}">
        {%- liquid
            assign product_per_row = section.settings.product_per_row
            assign product_per_row_tablet = section.settings.product_per_row_tablet
            assign product_per_row_mobile = section.settings.product_per_row_mobile
            assign product_limit = section.settings.limit
            assign collection = collections[section.settings.collection]
        -%}
        <slider-component>
          <div class="title-wrapper">
            <p class="heading-title h2">{{ section.settings.collection.title | escape }}</p>
            <div class="pagination-wrapper">
              {%- if section.settings.collection != blank -%}
                <a href="{{ section.settings.collection.url }}" class=" shop-products desktop-only link">
                  {{ 'sections.product-slider.shop' | t }}
                </a>
              {%- endif -%}
              <div class="pagination-custom">
                {%- liquid
                  assign products_to_display = collection.all_products_count

                  if collection.all_products_count > product_per_row
                    assign products_to_display = product_per_row 
                  endif
                -%}
                <div class="slider-counter caption hidden">
                  <span class="slider-counter--current">1</span>
                  <span aria-hidden="true"> / </span>
                  <span class="visually-hidden">{{ 'accessibility.of' | t }}</span>
                  <span class="slider-counter--total">{{ products_to_display }}</span>
                </div>
                <button id="prev-control-{{ section.id }}" type="button" class=" prev-{{ section.id }} pagination__item pagination__item--prev" name="previous" aria-label="{{ 'accessibility.previous_slide' | t }}"> {%- render 'icon-prev' -%}</button>
                <button id="next-control-{{ section.id }}" type="button" class=" next-{{ section.id }} pagination__item pagination__item--next" name="next" aria-label="{{ 'accessibility.next_slide' | t }}">{%- render 'icon-next' -%}</button>
              </div>
            </div>
            <a href="{{ section.settings.collection.url }}" class=" view-all link">
              {{ 'sections.featured_blog.view_all' | t }}
            </a>
          </div>
          <div class="sly-slider-{{ section.id }}">
            <ul id="list-item-{{ section.id }}" class="product-grid-slider grid--{{ product_per_row }}-col-desktop grid--{{ product_per_row_tablet }}-col-tablet grid--{{ product_per_row_mobile }}-col" >
              {% if section.settings.collection != blank %}
                {% for product in collection.products limit: product_limit %}
                  <li class="slider-li grid__item slider__slide grid-product">
                    {% render 'product-card',
                      product: product,
                      media_size: settings.image_ratio,
                      add_image_padding: section.settings.add_image_padding,
                      show_secondary_image: settings.show_secondary_image,
                      collection: section.settings.collection,
                    %}
                  </li>
                {% endfor %}
              {% else %}
                {% for i in (1..product_limit) %}
                  <li class="slider-li grid__item slider__slide grid-product">
                    {% render 'product-card-placeholder', 
                    product: product ,
                    media_size: settings.image_ratio,
                    show_secondary_image: settings.show_secondary_image,
                    add_image_padding: section.settings.add_image_padding %}
                  </li>
                {% endfor %}
              {% endif %}
            </ul>
          </div>
          <div class="scrollbar-product-slider">
            <div class="handle">
              <div class="mousearea"></div>
            </div>
          </div>
        </slider-component>
      </div>
    </div>
  </div>
</div>
<script>
// ====== sly slider init !!!
(function () {
		var $frame = $('.sly-slider-{{ section.id }}');
		var $wrap  = $frame.parent();

		// Call Sly on frame
		$frame.sly({
			horizontal: 1,
			itemNav: 'basic',
			smart: 1,
			activateOn: 'click',
			mouseDragging: 1,
			touchDragging: 1,
			releaseSwing: 1,
			startAt: 0,
			scrollBar: $wrap.find('.scrollbar-product-slider'),
			scrollBy: 1,
			speed: 1000,
			elasticBounds: 1,
      startPaused: false,
			easing: 'easeOutExpo',
			dragHandle: 1,
			dynamicHandle: 1,
			clickBar: 1,

			// Cycling
			cycleBy: false,

			// Buttons
			//prev: $wrap.find('.prev'),
			//next: $wrap.find('.next')
		});

    $wrap.find('.next-{{ section.id }}').on('click', function () {
      
      $frame.sly('slideBy', 1);
      
    }); 
    
    $wrap.find('.prev-{{ section.id }}').on('click', function () {
      //document.getElementById("next-control").removeAttribute("disabled");
      $frame.sly('slideBy', -1);
      
    });
	

    setInterval(function () {
        var myElement = document.querySelector('#list-item-{{ section.id }}');
        var style = window.getComputedStyle(myElement);
        var matrix = new WebKitCSSMatrix(style.transform);
        var widthSlide = parseFloat(document.getElementById('list-item-{{ section.id }}').style.width);
        var maxWidth = parseFloat($("#list-item-{{ section.id }}").width());
        
        if(widthSlide - maxWidth + matrix.m41 <= 5){
          document.getElementById("next-control-{{ section.id }}").disabled = "disabled";
        }else{
          document.getElementById("next-control-{{ section.id }}").removeAttribute("disabled");
        }

        if(matrix.m41>-1){
          document.getElementById("prev-control-{{ section.id }}").disabled = "disabled";
        }else{
        document.getElementById("prev-control-{{ section.id }}").removeAttribute("disabled");
      }
    }, 100);
}());
</script>
{% schema %}
{
  "name": "Product slideshow",
  "class": "spaced-section-100 spaced-top-100",
  "settings": [
    {
      "type": "header",
      "content": "Collection"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "product_per_row",
      "label": "Products per row",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "product_per_row_tablet",
      "label": "Products per row tablet",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "range",
      "id": "product_per_row_mobile",
      "label": "Products per row mobile",
      "info": "Apply to screen 480px -> 767px",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 2
    },
    {
      "type": "range",
      "id": "limit",
      "label": "Limit",
      "min": 1,
      "max": 20,
      "step": 1,
      "default": 12
    },
    {
			"type": "header",
			"content": "Common settings"
		},
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#FAFAFA"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 50,
      "max": 150,
      "step": 1,
      "unit": "px",
      "label": "Padding Top DeskTop",
      "default": 64
    }, 
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 50,
      "max": 150,
      "step": 1,
      "unit": "px",
      "label": "Padding Bottom Desktop",
      "default": 92
    }, 
    {
      "type": "header",
      "content": "Mobile settings"
    }, 
    {
      "type": "range",
      "id": "padding_top_mb",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Top Mobile",
      "default": 32
    }, 
    {
      "type": "range",
      "id": "padding_bottom_mb",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Bottom Mobile",
      "default": 32
    }
  ],
  "presets": [
    {
      "name": "Product slideshow",
      "category": "Product"
    }
  ]
}
{% endschema %}